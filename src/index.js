import React from 'react';
import ReactDOM from 'react-dom';
import Galeria from "./komponensek/Galeria";

ReactDOM.render(
  <React.StrictMode>
  <Galeria/>
  </React.StrictMode>,
  document.getElementById('root')
);
